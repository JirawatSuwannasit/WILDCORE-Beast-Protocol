name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  typecheck-test:
    name: Typecheck + Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm run ci:check
      - uses: actions/upload-artifact@v4
        with:
          name: web-build
          path: dist
          retention-days: 14

  vercel-preview:
    name: Vercel Web Preview
    needs: typecheck-test
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'pull_request' && vars.VERCEL_PROJECT_ID != '' }}
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Deploy Preview to Vercel
        id: deploy
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ vars.VERCEL_ORG_ID }}
          vercel-project-id: ${{ vars.VERCEL_PROJECT_ID }}
          github-comment: true

  android-debug-apk:
    name: Build Debug APK
    needs: typecheck-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - run: npm ci
      # Debug-only build mode (see src/systems/debugFlags.ts) so dev tools
      # like the F3-overlay double-jump toggle are reachable in this APK
      # but never in the default `npm run build` used by Vercel preview
      # and the eventual signed release AAB.
      - run: npm run build:debug
      - run: npx cap sync android
      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug --no-daemon
      - uses: actions/upload-artifact@v4
        with:
          name: wildcore-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
